# Purpose:       config file for zsh)
#
#Site        :https://renantmagalhaes.net
#Author      :Renan Toesqui Magalhães <renantmagalhaes@gmail.com>
#                                     <https://github.com/renantmagalhaes>
#
# ---------------------------------------------------------------

# Path to your oh-my-zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Load the tiny deferral scheduler before startup work. The installer keeps
# this plugin in Oh My Zsh's custom plugin directory.
ZSH_DEFER_PLUGIN="${ZSH_CUSTOM:-$ZSH/custom}/plugins/zsh-defer/zsh-defer.plugin.zsh"
[[ -r "$ZSH_DEFER_PLUGIN" ]] && source "$ZSH_DEFER_PLUGIN"
unset ZSH_DEFER_PLUGIN

# Load configs
[[ -f ~/.zsh/main.zsh ]] && source ~/.zsh/main.zsh
[[ -f ~/.zsh/programs.zsh ]] && source ~/.zsh/programs.zsh
[[ -f ~/.zsh/functions.zsh ]] && source ~/.zsh/functions.zsh
[[ -f ~/.zsh/extras.zsh ]] && source ~/.zsh/extras.zsh
source $ZSH/oh-my-zsh.sh
[[ -f ~/.zsh/aliases.zsh ]] && source ~/.zsh/aliases.zsh

# These two plugins are documented as safe zsh-defer candidates. Loading them
# after the prompt is available makes startup feel instant without deferring
# completion or prompt setup, which are more order-sensitive.
ZSH_AUTOSUGGESTIONS_PLUGIN="$ZSH_CUSTOM/plugins/zsh-autosuggestions/zsh-autosuggestions.zsh"
ZSH_SYNTAX_HIGHLIGHTING_PLUGIN="$ZSH_CUSTOM/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
for plugin in "$ZSH_AUTOSUGGESTIONS_PLUGIN" "$ZSH_SYNTAX_HIGHLIGHTING_PLUGIN"; do
  [[ -r "$plugin" ]] || continue
  if (( $+functions[zsh-defer] )); then
    zsh-defer source "$plugin"
  else
    source "$plugin"
  fi
done
unset ZSH_AUTOSUGGESTIONS_PLUGIN ZSH_SYNTAX_HIGHLIGHTING_PLUGIN plugin

# zoxide
if (( $+commands[zoxide] )); then
  if (( $+functions[zsh-defer] )); then
    # Run after the first idle cycle: zoxide is useful, but it needn't delay
    # the first prompt. Default zsh-defer refresh actions keep ZLE in sync.
    zsh-defer -t 0.05 -c 'eval "$(zoxide init zsh)"'
  else
    eval "$(zoxide init zsh)"
  fi
fi

# Oh my posh
eval "$(oh-my-posh init zsh --config $HOME/.config/omp/oh-my-posh-minimal.yaml)"
