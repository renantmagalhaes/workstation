# Workspace Configuration
# This file contains workspace-specific rules and settings

# Shared workspaces across all monitors (5 workspaces total)
workspace = 1, all
workspace = 2, all
workspace = 3, all
workspace = 4, all
workspace = 5, all

# Special workspace for scratchpad
workspace = special:magic, gapsout:50, opacity:0.9, border:false

# Workspace-specific window rules (if needed)
# Example: Force certain apps to specific workspaces
# windowrulev2 = workspace 1, class:^(kitty)$
# windowrulev2 = workspace 2, class:^(firefox)$
