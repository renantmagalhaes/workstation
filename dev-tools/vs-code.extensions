=> Dev Tools
    -> Beautify (ext install HookyQR.beautify)
    -> Docker (ext install ms-azuretools.vscode-docker)
    -> Kubernetes (ext install ms-kubernetes-tools.vscode-kubernetes-tools)
    -> Python (ext install ms-python.python)
        -> Select interpreter
        -> Verify pylint
    -> Python Extension Pack (ext install donjayamanne.python-extension-pack)
    -> Yaml (ext install redhat.vscode-yaml)
    -> Json (ext install ZainChen.json)
    -> shell-format (ext install foxundermoon.shell-format)
    -> AutoComplate shell (ext install truman.autocomplate-shell)
    ->  Markdown lint (ext install markdownlint)
    -> GitLens (ext install eamodio.gitlens)
    -> compareit (ext install in4margaret.compareit)
    -> live server (ext install ritwickdey.LiveServer)
    -> Bracket Pair Colorizer 2 (ext install CoenraadS.bracket-pair-colorizer-2)
    -> SQLTools (ext install mtxr.sqltools)


=> Themes
    -> Material Theme (ext install Equinusocio.vsc-material-theme)
    Set theme to Darker High Contrast
    -> Material Icon Theme (ext install PKief.material-icon-theme)
    Set accent collor > Acid Lime
